<UserControl x:Class="rupban.loginmodule.Views.ProjectsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Views="clr-namespace:rupban.loginmodule.Views"
    Height="223" Width="525">
    <Grid>
      <ListBox Margin="22,23,26,33" Name="projectListBox" ItemsSource="{Binding Path=ProjectList}" >
        <ListBox.ItemTemplate>
          <DataTemplate>
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="{Binding Path=Name}" />
              <Button Padding="10" Content="View RupbanBoard" Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type Views:ProjectsView}}, Path=DataContext.ViewRupbanBoardCommand}"/>
              
            </StackPanel>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Grid>
</UserControl>
